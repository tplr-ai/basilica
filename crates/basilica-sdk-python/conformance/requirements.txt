# FR-1 conformance: pinned SDK + test runner (NFR-8).
# If you bump `openai`, update OPENAI_SDK_PIN in test_openai_conformance.py in
# the same change and re-run the full suite.
# Relationship to the shipped SDK: ../pyproject.toml declares the
# tested-compatible range `openai>=2.46,<3`; this exact pin is the version
# within that range that the wire-contract suite verifies.
openai==2.46.0
# The mock gateway rides on httpx.MockTransport (openai's own HTTP layer);
# pinned for reproducible transport/streaming semantics.
httpx==0.28.1
pytest==9.1.1
