name: Insecure workflow fixture

on:
  # ruleid: basilica.github-actions.no-pull-request-target
  pull_request_target:

permissions:
  contents: read
  # ruleid: basilica.github-actions.no-packages-write
  packages: write

---

name: Safe workflow fixture

on:
  # ok: basilica.github-actions.no-pull-request-target
  pull_request:

permissions:
  contents: read
  # ok: basilica.github-actions.no-packages-write
  packages: read
